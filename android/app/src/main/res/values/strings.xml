<?xml version="1.0" encoding="utf-8"?>
<!--
  Настройки Meta SDK. Android-часть SDK читает их сама при старте приложения —
  из Dart сюда ничего не передаётся.

  Значения берутся в developers.facebook.com → приложение → Settings → Basic
  (App ID) и Settings → Advanced (Client Token). Секретом они не являются: оба
  лежат в открытом виде внутри установленного APK. А вот забыть их —
  как раз способ уронить релиз: без client token SDK на iOS падает при старте,
  на Android молча отбрасывает все события.

  Если сюда когда-нибудь вернутся заглушки REPLACE_WITH_META_*, релизная сборка
  в CI не пройдёт намеренно — см. job «preflight» в .github/workflows/release.yml.
-->
<resources>
    <string name="facebook_app_id" translatable="false">1113651381185008</string>
    <string name="facebook_client_token" translatable="false">f0aa999e7dd4a7c85f43c0c219ff161c</string>
</resources>
