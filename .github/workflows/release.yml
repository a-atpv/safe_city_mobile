name: Release

# Релиз клиентского приложения. Запускается тегом (git tag v1.0.3 && git push --tags)
# или руками через вкладку Actions.
#
# Что произойдёт без секретов: Android соберётся с debug-подписью (в Play такую
# не принять), iOS соберётся без подписи — как проверка, что проект компилируется.
# Оба шага честно напишут warning в лог. Список секретов — в CI.md.

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      publish:
        description: 'Заливать в сторы (иначе только собрать артефакты)'
        type: boolean
        default: false

permissions:
  contents: read

env:
  FLUTTER_CHANNEL: stable

  # Номер сборки = отступ + номер прогона воркфлоу. Стору важно ровно одно:
  # каждый следующий билд строго больше предыдущего. Отступ 100 взят с запасом
  # над однозначными номерами, которые заливались руками. Если что-то уедет в
  # стор в обход CI — поднять отступ, чтобы нумерация не столкнулась.
  BUILD_NUMBER_OFFSET: 100

  PACKAGE_NAME: com.safeCity.appname

jobs:
  android:
    name: Android
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17' # AGP 8.9.1 требует 17+

      - uses: subosito/flutter-action@v2
        with:
          channel: ${{ env.FLUTTER_CHANNEL }}
          cache: true

      - run: flutter pub get

      - name: Проверить, какие секреты заданы
        id: gate
        env:
          KEYSTORE_B64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          PLAY_SA_JSON: ${{ secrets.PLAY_SERVICE_ACCOUNT_JSON }}
        run: |
          if [ -n "$KEYSTORE_B64" ]; then
            echo "signing=true" >> "$GITHUB_OUTPUT"
          else
            echo "signing=false" >> "$GITHUB_OUTPUT"
            echo "::warning::ANDROID_KEYSTORE_BASE64 не задан — сборка уйдёт с debug-подписью. Google Play такую не примет, и поверх установленного приложения она не встанет."
          fi
          if [ -n "$PLAY_SA_JSON" ]; then
            echo "play=true" >> "$GITHUB_OUTPUT"
          else
            echo "play=false" >> "$GITHUB_OUTPUT"
            echo "::warning::PLAY_SERVICE_ACCOUNT_JSON не задан — в Google Play заливать нечем, AAB останется артефактом прогона."
          fi

      - name: Восстановить keystore
        if: steps.gate.outputs.signing == 'true'
        env:
          KEYSTORE_B64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          STORE_PASSWORD: ${{ secrets.ANDROID_STORE_PASSWORD }}
          KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
          # Переменная, а не секрет: алиас — это имя, а не тайна, зато секретом
          # GitHub маскирует его по всему логу. Алиас «upload» превращал строку
          # actions/upload-artifact в actions/***-artifact.
          KEY_ALIAS: ${{ vars.ANDROID_KEY_ALIAS || 'upload' }}
        run: |
          # storeFile в build.gradle.kts резолвится относительно android/app,
          # поэтому и файл кладём туда же.
          printf '%s' "$KEYSTORE_B64" | base64 -d > android/app/upload-keystore.jks
          cat > android/key.properties <<EOF
          storeFile=upload-keystore.jks
          storePassword=$STORE_PASSWORD
          keyPassword=$KEY_PASSWORD
          keyAlias=$KEY_ALIAS
          EOF

      - name: Версия и номер сборки
        id: build
        run: |
          NUMBER=$(( BUILD_NUMBER_OFFSET + GITHUB_RUN_NUMBER ))
          # Версию задаёт тег: v1.0.3 → 1.0.3. То есть поднять версию — это назвать
          # тег, а не править pubspec; автоматически инкрементировать её нельзя,
          # потому что машина не знает, патч это или минор. Ручной запуск без тега
          # берёт версию из pubspec.yaml.
          NAME="${GITHUB_REF#refs/tags/v}"
          if [ "$NAME" = "$GITHUB_REF" ]; then
            NAME=$(sed -n 's/^version: *\([^+]*\).*/\1/p' pubspec.yaml)
          fi
          echo "number=$NUMBER" >> "$GITHUB_OUTPUT"
          echo "name=$NAME" >> "$GITHUB_OUTPUT"
          echo "→ версия $NAME, номер сборки $NUMBER"

      - name: Собрать AAB и APK
        run: |
          ARGS="--build-name=${{ steps.build.outputs.name }} --build-number=${{ steps.build.outputs.number }}"
          flutter build appbundle --release $ARGS
          flutter build apk --release $ARGS

      # Debug-подписанный релиз внешне неотличим от нормального — ровно до
      # момента, когда Play отвергнет загрузку. Поэтому спрашиваем APK, чем он
      # подписан, и сверяем с ожидаемым отпечатком.
      - name: Проверить, чем подписан APK
        if: steps.gate.outputs.signing == 'true'
        env:
          EXPECTED: ${{ vars.ANDROID_CERT_SHA256 }}
        run: |
          APKSIGNER=$(ls "$ANDROID_HOME"/build-tools/*/apksigner | sort -V | tail -1)
          ACTUAL=$("$APKSIGNER" verify --print-certs build/app/outputs/flutter-apk/app-release.apk \
            | grep -i 'certificate SHA-256 digest' | head -1 | awk '{print $NF}')
          echo "Отпечаток подписи: $ACTUAL"
          if [ -z "$EXPECTED" ]; then
            echo "::warning::Переменная ANDROID_CERT_SHA256 не задана — сверять не с чем. Задайте её этим значением, чтобы подмена ключа ловилась автоматически."
          elif [ "$ACTUAL" != "$EXPECTED" ]; then
            echo "✗ Подписано не тем ключом. Ожидали $EXPECTED" >&2
            exit 1
          else
            echo "✓ Ключ тот самый."
          fi

      - uses: actions/upload-artifact@v7
        with:
          name: android-${{ steps.build.outputs.number }}
          retention-days: 30
          path: |
            build/app/outputs/bundle/release/app-release.aab
            build/app/outputs/flutter-apk/app-release.apk

      # Первую сборку в трек всё равно придётся залить руками: Play API умеет
      # обновлять уже существующее приложение, но не создавать его.
      #
      # alpha — это и есть «Закрытое тестирование» в консоли. Названия треков в
      # API и в интерфейсе не совпадают: internal = Внутреннее, alpha = Закрытое,
      # beta = Открытое. Если заведён именованный закрытый трек, его идентификатор
      # задаётся переменной PLAY_TRACK.
      - name: Залить в Google Play
        if: steps.gate.outputs.play == 'true' && steps.gate.outputs.signing == 'true' && (github.event_name == 'push' || inputs.publish)
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.PLAY_SERVICE_ACCOUNT_JSON }}
          packageName: ${{ env.PACKAGE_NAME }}
          releaseFiles: build/app/outputs/bundle/release/app-release.aab
          track: ${{ vars.PLAY_TRACK || 'alpha' }}
          status: completed

  ios:
    name: iOS
    # macos-latest, а не конкретная версия: устаревшие метки GitHub со временем
    # выключает, и пайплайн, который трогают раз в месяц, падал бы на ровном месте.
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v7

      - uses: subosito/flutter-action@v2
        with:
          channel: ${{ env.FLUTTER_CHANNEL }}
          cache: true

      - run: flutter pub get

      - name: Проверить, какие секреты заданы
        id: gate
        env:
          ASC_KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          ASC_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
          ASC_PRIVATE_KEY: ${{ secrets.APP_STORE_CONNECT_PRIVATE_KEY }}
        run: |
          if [ -n "$ASC_KEY_ID" ] && [ -n "$ASC_ISSUER_ID" ] && [ -n "$ASC_PRIVATE_KEY" ]; then
            echo "signing=true" >> "$GITHUB_OUTPUT"
          else
            echo "signing=false" >> "$GITHUB_OUTPUT"
            echo "::warning::Ключа App Store Connect нет — соберём только неподписанный билд как проверку компиляции. В TestFlight уходить нечему."
          fi

      - name: Версия и номер сборки
        id: build
        run: |
          NUMBER=$(( BUILD_NUMBER_OFFSET + GITHUB_RUN_NUMBER ))
          NAME="${GITHUB_REF#refs/tags/v}"
          if [ "$NAME" = "$GITHUB_REF" ]; then
            NAME=$(sed -n 's/^version: *\([^+]*\).*/\1/p' pubspec.yaml)
          fi
          echo "number=$NUMBER" >> "$GITHUB_OUTPUT"
          echo "name=$NAME" >> "$GITHUB_OUTPUT"
          echo "→ версия $NAME, номер сборки $NUMBER"

      - name: Проверочная сборка без подписи
        if: steps.gate.outputs.signing == 'false'
        run: flutter build ios --release --no-codesign --build-name=${{ steps.build.outputs.name }} --build-number=${{ steps.build.outputs.number }}

      - name: Положить ключ App Store Connect
        if: steps.gate.outputs.signing == 'true'
        env:
          ASC_KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          ASC_PRIVATE_KEY: ${{ secrets.APP_STORE_CONNECT_PRIVATE_KEY }}
        run: |
          # ~/private_keys — одно из мест, где altool сам ищет ключ по его Key ID.
          mkdir -p ~/private_keys
          printf '%s' "$ASC_PRIVATE_KEY" | base64 -D > ~/private_keys/AuthKey_$ASC_KEY_ID.p8
          chmod 600 ~/private_keys/AuthKey_$ASC_KEY_ID.p8

      - name: Собрать и подписать архив
        if: steps.gate.outputs.signing == 'true'
        env:
          ASC_KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          ASC_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
        run: |
          # Flutter-часть собирается отдельно, потому что архив делаем сами:
          # только своим вызовом xcodebuild можно передать -allowProvisioningUpdates,
          # по которому Xcode выпустит сертификат и профиль на чистом раннере.
          flutter build ios --release --no-codesign --build-name=${{ steps.build.outputs.name }} --build-number=${{ steps.build.outputs.number }}

          xcodebuild -workspace ios/Runner.xcworkspace \
            -scheme Runner \
            -configuration Release \
            -archivePath build/Runner.xcarchive \
            -allowProvisioningUpdates \
            -authenticationKeyPath ~/private_keys/AuthKey_$ASC_KEY_ID.p8 \
            -authenticationKeyID "$ASC_KEY_ID" \
            -authenticationKeyIssuerID "$ASC_ISSUER_ID" \
            archive

          xcodebuild -exportArchive \
            -archivePath build/Runner.xcarchive \
            -exportOptionsPlist ios/ExportOptions.plist \
            -exportPath build/ipa \
            -allowProvisioningUpdates \
            -authenticationKeyPath ~/private_keys/AuthKey_$ASC_KEY_ID.p8 \
            -authenticationKeyID "$ASC_KEY_ID" \
            -authenticationKeyIssuerID "$ASC_ISSUER_ID"

      - uses: actions/upload-artifact@v7
        if: steps.gate.outputs.signing == 'true'
        with:
          name: ios-${{ steps.build.outputs.number }}
          retention-days: 30
          path: build/ipa/*.ipa

      - name: Залить в TestFlight
        if: steps.gate.outputs.signing == 'true' && (github.event_name == 'push' || inputs.publish)
        env:
          ASC_KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          ASC_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
        run: |
          IPA=$(find build/ipa -name '*.ipa' | head -1)
          if [ -z "$IPA" ]; then
            echo "✗ .ipa не найден — экспорт архива не отдал файл." >&2
            exit 1
          fi
          xcrun altool --upload-app -f "$IPA" -t ios \
            --apiKey "$ASC_KEY_ID" --apiIssuer "$ASC_ISSUER_ID"
